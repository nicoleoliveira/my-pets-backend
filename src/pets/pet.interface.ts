export interface Pet {
    id?: number,
    name: string,
    species: string,
    birthdate: string,
    isDeleted: boolean,
    createdDate?: string,
    updatedDate?: string,
    removedDate?: string
}
